const { json } = require('express');
const { db } = require('../Start/mysql');
const completed = {}
var resultss = [];
completed.listReport = async (req, res) => {

    //get list of reports

};

module.exports = completed;