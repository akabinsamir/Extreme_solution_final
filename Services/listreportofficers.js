const { json } = require('express');
const { db } = require('../Start/mysql');
const listofficers = {}
var resultss = [];
listofficers.listReport = async (req, res) => {

    //get list of reports

};

module.exports = listofficers;